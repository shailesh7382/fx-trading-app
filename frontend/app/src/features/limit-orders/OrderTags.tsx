/**
 * The order blotter's tags now live in `@/shared/trading/Tags`, shared with the rate
 * grid and the trade blotter. This module keeps the original import path working.
 */
export { DirectionTag, FieldRow, MetaTag, StatusTag } from '@/shared/trading/Tags';
